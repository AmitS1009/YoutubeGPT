# Mostly handled by FAISS, but kept for structure compliance
class MetadataStore:
    """
    Helper class for metadata filtering or external metadata storage if needed.
    Currently, metadata is stored directly in the VectorStore chunks.
    """
    pass
